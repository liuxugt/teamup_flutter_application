import 'package:flutter/material.dart';

class SelectCoursePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Course Select Page"),),
    );
  }
}
